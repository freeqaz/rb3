#!/usr/bin/env bash
# Combined objdiff + Ghidra + m2c analysis for RB3 decompilation work
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/../tools/analyze_function.py" "$@"
