#!/usr/bin/env bash
# Pre-flight static link-error linter for RB3 source promotions.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/../tools/lint_link_issues.py" "$@"
