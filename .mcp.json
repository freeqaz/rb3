{
  "mcpServers": {
    "orchestrator": {
      "command": "python3",
      "args": ["-m", "scripts.orchestrator.mcp_server", "--db", "decomp.db"],
      "env": {
        "PYTHONPATH": "${PWD}"
      }
    }
  }
}
