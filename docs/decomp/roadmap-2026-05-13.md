# Decomp Roadmap — 2026-05-13

Synthesized from four exploration agents run after fixing the 4 stale-COMPLETE regressions earlier today. Project state: **30,260 / 41,254 functions matched (73.35%), 57.06% code matched.**

## Tier 0 — Already Done This Session

- `CharHair::Cross` 90.88% → 100% (Vec.h dst.Set form, concurrent commit 5a782ab3)
- `SongSort::BuildSongList` 77.08% → 100% (push_front pattern)
- `SetlistSort::BuildSetlistList` 80.98% → 100% (push_front pattern)
- `WPAD::OnShutdown` 62.05% → 100% (inlined cleanup helpers + decl order)
- `PhraseAnalyzer::Verify` 98.39% → 99.78% (i11/bit decl order)
- `MakeBSPTree` (Geo.cpp) 93.33% → 94.26% (push_front pattern)

## Tier 1 — Trivial Single-Blocker Units (highest ROI)

Each one-function fix unlocks unit-level 100% (LINKED status enables ELF cross-validation). All are already at 99-100% so stragglers are register-drift only.

| Unit | Total | @100% | Straggler | Hint |
|---|---|---|---|---|
| `band3/meta_band/BandProfile` | 111 | 110 | `FakeProfileFill()` | one-fn drift, large unit |
| `network/net/SessionMessages` | 81 | 80 | `JoinRequestMsg::Load()` | BinStream pattern |
| `network/Platform/Log` | 8 | 7 | `Quazal::Log::OutputImpl()` | small unit |
| `system/synth/tomcrypt/aes` | 5 | 4 | `rijndael_setup()` | crypto, watch for tables |
| `band3/meta_band/NetSync` | 34 | 33 | `NetSync::IsTransitionAllowed()` | state machine |
| (+ 6 more in agent 2 report rows 6-15) | | | | |

**Expected throughput**: ~5 units/wave at high success rate.

## Tier 2 — Near-100% Unit Cluster (95 EASY units)

After Tier 1, sweep the remaining 95 EASY units (99-100% ready) using:
```bash
python3 scripts/batch_check.py 'system/*' --max-partial-pct 99.9
```
Pattern: each unit has 1-3 stragglers in the 99-100% range. Most are register drift fixable via decl-order tricks (today's `chan`/`intrStatus` and `i11`/`bit` patterns).

## Tier 3 — Push_front pattern hunt result

Only **1 candidate** found beyond what we fixed today (the codebase mostly uses `std::vector`, not `std::list`):
- `Geo.cpp:791` `MakeBSPTree` — applied, +0.93% (now 94.26%). Doesn't go all the way to 100% because the remaining diff is unrelated.

**Pattern is largely exhausted.** Don't expect more wins here.

## Tier 4 — High-Leverage Large-Function Clusters

For waves doing real decomp work (not just polish):

### Cluster A — `band3/tour/TourPerformerLocal` (9 fns, ~8.7KB unmatched)
Most untouched (avg 4.2% fuzzy). Probably empty/stub bodies. Use `bin/analyze-function` + DC3 reference (Milo engine should share patterns). Best for a single dedicated wave.

### Cluster B — `system/char/CharHair` (12 fns, ~16KB unmatched)
STL instantiations from the BSP/hair simulation code. DC3 has nearly-complete versions of these — check `dc3_compare.py --filter system/char/`. Likely template-instantiation matching.

### Cluster C — `sdk/RVL_SDK/src/wpad/WPAD` (12 fns, ~17KB unmatched)
Wii Remote callbacks: `WPADControlSpeaker`, `WPADControlDpd`, `__wpadMplsCallback`, etc. These follow Wii SDK patterns we now know (extern "C", inline cleanup helpers, etc.). Reference `og-rb3/src/sdk/RVL_SDK/src/wpad/WPAD.c`.

### Top single-function quick wins (large + partial match)
- `MetaPanel::Init` — 4,668 bytes at 43% (~2.7KB to gain)
- `PerformanceData::SaveFixed` — 3,756 bytes at 39% (~2.3KB to gain)
- `VocalPlayer::Poll` — 8,668 bytes at 1.6% (8.5KB to gain, untouched — biggest single function in this list)
- `WiiMultiMesh::DrawShowing` — 5,036 bytes at 0.1% (5KB to gain, untouched)

## Tier 5 — Skip

- **107 stale-COMPLETE entries** with <1% drops (mostly RndText cluster from wave 36's `int dump` scope change). All register-drift noise. Fixing would require ugly hints with zero real match gain.
- **146 AT_LIMIT units** in agent 2's "BLOCKED" pile. Confirmed unfixable.

## Suggested Order of Operations

1. **Next wave (~3-4 hr Sonnet time)**: Tier 1 top-5 units. Each is one-function. Should land ~5 unit-100% closures.
2. **Wave +1**: Tier 2 sweep across `system/*` and `band3/meta_band/*` with batch_check. Expect 10-20 more units to close.
3. **Wave +2**: Pick ONE Tier 4 cluster (recommend Cluster C/WPAD — patterns established today). Dedicate full focus, expect 3-5 large-function decomps.
4. **Long tail**: Tier 4 single-function quick wins as opportunities arise.
5. **Never**: Tier 5.

## Tooling Notes

- None of Tier 4 candidates have Ghidra decomp cached. Run `/analyze-function SYMBOL` to populate caches before attempting.
- `bin/analyze-function -u UNIT SYMBOL` is the entry point for any new function.
- Tier 2 register-drift fixes don't usually need Ghidra; comparing target/base asm is enough.
