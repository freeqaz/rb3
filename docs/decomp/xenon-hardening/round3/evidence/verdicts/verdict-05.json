{
  "pair": 5,
  "verdict": "same",
  "confidence": "high",
  "claimed_wii_symbol": "AddUV__Q213BandPatchMesh8MeshVertFPCQ213BandPatchMesh8MeshVertRC7Vector2PC7Vector2",
  "category": "system",
  "stratum": "BSIM>=30",
  "reasoning": "The Wii m2c and Xenon pseudo-C describe the same source function (BandPatchMesh::MeshVert::AddUV) with structural agreement across the MWCC/MSVC compiler boundary that is far too detailed to be a sibling-alias coincidence. (1) IDENTICAL CONTROL-FLOW SHAPE: both compute the three vertex-position deltas (this->vert - mv->vert in x/y/z), a dot-product projection scalar fVar6/temp_f1 = -(...), the squared-length comparison with a leaf two-magnitude branch (if projected-len > 0: normalize, else if orig-len > 0: return 0), an inverse-sqrt Newton refinement, an optional arg2!=NULL UV-distance early-return (return 0), and a final identical 8-field accumulation. (2) MATCHING CONSTANT POOL: the rsqrt refinement uses the exact same magic constants -- 3.0 (Wii @F_00004040 / Xenon DAT_8201419c) and 0.5 (Wii @F_0000003f / Xenon DAT_8200108c) and 1.0 (@F_0000803f) in the same Newton-Raphson 1/sqrt formula `-(x*x*v - 3.0)*x*0.5`. (3) FIELD OFFSETS AGREE: both treat the vert struct as {ptr@[0], 3x3-ish basis@[1..7], UV-accum@[9],[10]} and the final accumulation adds exactly fields [1],[2],[3],[5],[6],[7] plus the UV pair -- the same skip pattern (skips the [0] pointer, [4], and [8]). (4) The Wii body's referenced strings (BandPatchMesh.cpp, asserts 'this != mv', 'mv->mVert') positively confirm the claimed symbol; the Xenon leaf has its MSVC asserts/strings inlined/stripped (expected cross-compiler artifact) and references none that contradict. No genuine discriminator (no differing node-size literal, no valid-vs-valid type tag mismatch, no contradicting partner string). The only differences are expected MWCC-vs-MSVC codegen artifacts (psq_/paired-single vs scalar, fmadds fusion, the inlined assert calls present on Wii, register/schedule). Verdict SAME.",
  "key_evidence": "Identical normalize-and-accumulate algorithm with matching Newton 1/sqrt constants (3.0, 0.5, 1.0), matching field-offset layout and the exact same [1,2,3,5,6,7]+UV accumulation skip pattern, plus arg2!=NULL distance-gate early-return; Wii strings positively name BandPatchMesh.cpp / 'this != mv' / 'mv->mVert', no contradicting Xenon strings or discriminating literals."
}
