{
  "pair": 27,
  "verdict": "same",
  "confidence": "high",
  "claimed_wii_symbol": "StopToListen__Q26Quazal14ObjDupProtocolFv",
  "category": "network",
  "stratum": "BSIM 15-20",
  "reasoning": "The Xenon body (0x82a69020) and the Wii StopToListen body share an essentially identical control-flow skeleton and field-offset signature, with the single most-reliable resolved callee agreeing across the toolchain boundary. (1) STRUCTURE: both functions are built from up-to-four guarded instance-lookup blocks, each one opening with a GetCurrentContext call followed by an InstanceControl-table lookup, gated by boolean member flags, with member-flag resets after the gated blocks, and closing with a single call on a sub-object embedded at +8. (2) FIELD OFFSETS match exactly where not subject to struct packing: the gate at offset 5 (Wii this->unk5 / Xenon iStack+5), the gate at offset 4 (this->unk4 / iStack+4), the '*flag = 0' resets at offsets 5 and 4 after each gated block, the 0x40 then 0x10 indirection on the looked-up instance, and the closing call taking 'this + 8' (Wii CancelPendingCalls(&this->unk8) on the CallRegister member). (3) RESOLVED CALLEE AGREEMENT: Xenon 0x82a414b8 -> GetCurrentContext__Q26Quazal15PseudoSingletonFv, which is exactly the call the Wii body issues at the head of each lookup block; this named library-singleton accessor agreeing is a strong, hard-to-fake cross-compiler signal. (4) The only divergences are validated systematic MWCC-vs-MSVC artifacts: struct-packing offset shifts (Wii unk48/unk4A vs Xenon 0x4c/0x4e on the same trailing virtual-dispatch args; vtable slot rendered +0x18 vs +0x10) and MSVC inlining the GetInstance(s_oInstanceTable, N) lookup chain into single helper calls (Function_823D7DC8(0x82ddde90 = the Xenon s_oInstanceTable, N, context)). (5) NO STRING REFUTATION: zero referenced strings, and no callee resolves to a competing sibling/template partner of StopToListen. CAVEAT (does not change verdict): two callees carry imperfect BSim labels -- 0x823d7dc8 -> HasStartedTermination and 0x82a8db10 -> Cancel(DOCallContext) -- but these are best read as unreliable BSim resolutions of small inlined/sibling helpers on the Xenon side; the 0x82a8db10 call site passes 'this+8' (the CallRegister sub-object) which structurally matches CancelPendingCalls, and neither label names a discriminating literal (node-size, valid-vs-valid type tag) or a contradicting partner that would indicate a same-TU sibling alias.",
  "key_evidence": [
    "Xenon callee 0x82a414b8 -> GetCurrentContext__Q26Quazal15PseudoSingletonFv: agrees with the Wii body's per-block opening call (strongest signal).",
    "Identical control-flow skeleton: up-to-4 guarded instance-lookup blocks, each preceded by GetCurrentContext + InstanceControl-table lookup, gated by boolean member flags.",
    "Exact field-offset matches: gate flags at offsets 4 and 5, '*flag = 0' resets at 4 and 5, 0x40->0x10 instance indirection, trailing call on sub-object at +8 (Wii CancelPendingCalls(&this->unk8)).",
    "Divergences are validated systematic artifacts only: struct-packing shifts (0x48/0x4A vs 0x4c/0x4e; vtable slot 0x18 vs 0x10) and MSVC inlining of the GetInstance lookup chain (Function_823D7DC8 takes 0x82ddde90 = Xenon s_oInstanceTable).",
    "Zero referenced strings; no callee names a competing sibling/template partner; no discriminating node-size or type-tag literal differs.",
    "Soft caveat: 0x823d7dc8->HasStartedTermination and 0x82a8db10->Cancel(DOCallContext) are imperfect BSim labels on inlined/helper callees, not refutations of the outer-function identity."
  ]
}
