# W2.6.S3 — Verify (flag-OFF byte-identical + flag-ON gates + STOP-tripwire) — go/no-go package

_Generated by the W2.6.S3 implementer (Opus), 2026-07-06. Build: `rb3-native` + `rb3-tests` @ engine
pin `6221a56` (`native/build-agent-W2.6`, Clang). Depends on S1 (`0f3388c0`) + S2 (`2675cde6`)._

## Recommendation: **NO-FLIP** (there is nothing to flip)

PART-1 landed **diagnosis-only** (S1 `BRANCH=STOP`, `S1_DIAGNOSIS.md`); S2 was a **NO-OP** — the
`RB3_FOOT_REST_CAPTURE` flag was **never introduced** and **zero source lines changed** in the W2.6
lane (`git diff HEAD -- src/system/bandobj/BandCharacter.{cpp,h}` is empty). S3's "(only if S2 ran)"
premise is therefore not met for a flag A/B: **Gate 2 (flag-ON drop clears) and Gate 5 (flag-ON lineup)
are N/A — no flag exists.** The default-flip commit that this package would normally gate does not
exist and must NOT be created.

What S3 *can* and *does* certify: the **diagnosis-only HEAD** (which IS the "flag-OFF" state, byte-for-
byte) is **regression-safe** and **byte-identical to the S1 baseline**. All applicable regression nets
are green. Exit criteria 1 and 5 hold as PLAN.md predicted ("no new flag, no new code path").

## Gate results

| Gate | Applies? | Verdict | Evidence |
|---|---|---|---|
| **1. flag-OFF byte-identical** | YES | **PASS** | `git diff HEAD` on `BandCharacter.{cpp,h}` = EMPTY (no source delta ⇒ default path is the unchanged HEAD, identical to the S1 characterization). Drawlog splash canonical golden **PASS** (888 draws, 238 known-residual within bound, 0 unexpected). W0.5 lineup **PASS** all layers. rb3-tests **79 pass / 3 skip(fixture) / 0 fail** incl. `HandsBindOracle` green + fail-red RED. |
| **2. flag-ON drop clears** | **N/A** | — | No `RB3_FOOT_REST_CAPTURE` flag (S2 NO-OP). Nothing to turn ON. |
| **3. STOP-tripwire (max SKINPOS <92u, no 200-460u smear)** | YES (inherited) | **PASS** | S1 measured on this exact build: max `[REBIND_DRAW_SKINPOS]` = **69.5u**, `[REBIND_DRAW_FLING]` total = **0**, nothing ≥92u, nothing in 200-460u. No code changed since ⇒ unchanged. |
| **4. negative control (crowd/extras SKIN_CLAMP byte-identical)** | YES (trivial) | **PASS** | No source change touches the non-rebound crowd/extras path ⇒ SKIN_CLAMP is byte-identical to the S1 baseline (`parsed-drop.json`: 35 meshes / 1778 events; `parsed-default.json`: 36 / 2753 — lineup-dependent, unchanged). |
| **5. W0.5 lineup PASS (flag-ON path)** | reduces to flag-OFF=HEAD | **PASS** | `lineup-gate.py` verdict=**PASS** img/segA/ratioB/countC/pin; `max_band_ratio=3.99` within golden bound; 0 black frames; 4/4 frames PASS. |

## Verification runs (this session, against `native/build-agent-W2.6`)

**rb3-tests (criterion 5 — the standing char-bind regression net):**
- `./rb3-tests` → **79 PASSED / 3 SKIPPED / 0 FAILED** (988 ms). Skips are all fixture-dependent
  (`DrawLogGolden.PopulatesFromRealDrawMesh`, `HandsBindOracle.RealPathFixture`,
  `PlacementOracle.RealCaptureSpansBowl`) — expected, no in-game fixture dumped.
- `HandsBindOracle` GREEN: `ComposeIdentityAtBindPose` OK, `SkinnedVertsMatchAuthored` OK,
  `PerturbationIsDetected` OK, `RealPathFixture` SKIPPED (math/compose arm covers the invariant).
- `HandsBindOracle` FAIL-RED (`HANDS_BIND_ORACLE_PERTURB=1`): `ComposeIdentityAtBindPose` +
  `SkinnedVertsMatchAuthored` flip to **FAILED** → the oracle is live, not vacuously green.

**drawlog splash canonical golden (Gate 1):**
- `drawlog-golden.py --fixed-clock --canonical-order --scene splash_screen` →
  **PASS** (888 draws match golden; 238 known-residual divergences within bound, non-blocking).
  (The `rc=-11` line is the pre-existing bounded-boot teardown SIGSEGV documented in W0.3.STATUS S1;
  the dump was written before teardown — not a regression.)

**W0.5 lineup gate (Gate 1/5):**
- `lineup-gate.py --bin …W2.6/rb3-native` → **LINEUP_GATE verdict=PASS** img=PASS segA=PASS ratioB=PASS
  countC=PASS pin=PASS. Per-frame: 4/4 non-black, segA sliv 0-3 / ncomp 23-30, countC PASS. `verdict.json`
  → `/tmp/rb3-lineup-w26s3/verdict.json`.

## Why this is the correct honest outcome

S1's diagnosis refuted the PLAN's PART-1 mechanism (`missWhy="clipPlaying"` mid-clip first-resolve →
V24 drop): **0 `clipPlaying` pendings** on HEAD and in the W2.2 baseline; the load-time clip-free seed
already covers the shoe/leg bones (`[REST_SEED] poll=0` seeds 105-109 bones/member, later `added=0`);
`saddleshoe_skin.2` completes its rebind (`rebound=1`); the residual is a benign 2/155-frame count-in
transient the V24 guard already hides (max SKINPOS 69.5u, 0 fling). The only remaining bind-side lever
is `own==bound` rebake = the proven 200-460u rotation-basis STOP-TRIPWIRE, and the engine-side real fix
(defer-draw / lineup-aware guard) is out of PART-1's READ-ONLY scope. So S2 was correctly a NO-OP and
there is no behavior to gate or flip. This is the "no-fix outcome is an acceptable, honest result"
branch the plan explicitly anticipated.

## Handoff

- **No default-flip commit.** Do not add or flip `RB3_FOOT_REST_CAPTURE`.
- **S4 (PART 2 flag-registry):** register only `RB3_HANDS_BIND_FIX` + `RB3_SKEL_REBIND_FULL` + the 90
  `rb3/src/system` scan-root flags. Do **NOT** register `RB3_FOOT_REST_CAPTURE` (never implemented) —
  per S1/S2 handoff.
